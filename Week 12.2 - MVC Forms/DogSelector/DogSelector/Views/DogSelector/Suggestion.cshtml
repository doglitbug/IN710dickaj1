@model DogSelector.Models.Dog

@{
    Layout = null;
}

@functions{
    /// <summary>
    /// Get the string of an enum
    /// </summary>
    /// <param name="value">Value of the enum</param>
    /// <returns>String value of the enum</returns>
    private String getEnumString(Enum value)
    {
        System.Type type = value.GetType();
        return Enum.GetName(type, value);
    }

}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Dog Selector</title>

    <!-- Bootstrap -->
    <link href="../Content/bootstrap.min.css" rel="stylesheet">
    <link href="../Content/myStyles.css" rel="stylesheet" />

</head>
<body>
    <div class="container">
        <h1>Our recommendation</h1>
        <h2>We suggest the @Model.DisplayName</h2>
        <div class="row">
            <div class="col-sm-6"><img src="../Content/images/@Model.ImageName" class="dogPicture" /></div>
            <div class="col-sm-6">
                <table class="table table-striped table-bordered">
                    <tr><td>Activity level:</td><td>@getEnumString(@Model.ActivityLevel)</td></tr>
                    <tr><td>Shedding level:</td><td>@getEnumString(@Model.SheddingLevel)</td></tr>
                    <tr><td>Grooming level:</td><td>@getEnumString(@Model.GroomingLevel)</td></tr>
                    <tr><td>Intelligence level:</td><td>@getEnumString(@Model.IntelligenceLevel)</td></tr>
                    <tr><td>Good with Children:</td><td>@Model.GoodWithChildren</td></tr>
                    <tr><td>Drools:</td><td>@Model.Drools</td></tr>
                    <tr><td>Coat Length:</td><td>@getEnumString(@Model.CoatLength)</td></tr>
                    <tr><td>Size:</td><td>@getEnumString(@Model.Size)</td></tr>
                </table>
            </div>
        </div>
        <div>
            <!-- //TODO Wire up this button to actually do something! -->
            <button type="submit" class="btn btn-default">Choose another dog</button>
        </div>
    </div><!-- /container-->
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="../Scripts/bootstrap.min.js"></script>
</body>
</html>